== The Insulator Store

= Development

* pull down from github
* bundle install
* rake db:create (with postgres running)

= Deploying

* add heroku remote (git remote add heroku git@heroku.com:insulators.git)
* deploy (git push heroku master)